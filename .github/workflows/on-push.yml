name: on-push

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: corretto
          java-version: 17

      - name: Build
        env:
          super_secret: ${{ secrets.COOKIE }}
        run: |
          chmod +x ./gradlew
          ./gradlew clean
          ./gradlew build